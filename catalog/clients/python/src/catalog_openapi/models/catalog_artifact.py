
"""Model Catalog REST API

REST API for Model Registry to create and manage ML model metadata

The version of the OpenAPI document: v1alpha1
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
from typing import Any

from pydantic import BaseModel, ConfigDict, ValidationError, field_validator
from typing_extensions import Self

from catalog_openapi.models.catalog_metrics_artifact import CatalogMetricsArtifact
from catalog_openapi.models.catalog_model_artifact import CatalogModelArtifact

CATALOGARTIFACT_ONE_OF_SCHEMAS = ["CatalogMetricsArtifact", "CatalogModelArtifact"]

class CatalogArtifact(BaseModel):
    """A single artifact in the catalog API.
    """
    # data type: CatalogModelArtifact
    oneof_schema_1_validator: CatalogModelArtifact | None = None
    # data type: CatalogMetricsArtifact
    oneof_schema_2_validator: CatalogMetricsArtifact | None = None
    actual_instance: CatalogMetricsArtifact | CatalogModelArtifact | None = None
    one_of_schemas: set[str] = { "CatalogMetricsArtifact", "CatalogModelArtifact" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator("actual_instance")
    def actual_instance_must_validate_oneof(cls, v):
        instance = CatalogArtifact.model_construct()
        error_messages = []
        match = 0
        # validate data type: CatalogModelArtifact
        if not isinstance(v, CatalogModelArtifact):
            error_messages.append(f"Error! Input type `{type(v)}` is not `CatalogModelArtifact`")
        else:
            match += 1
        # validate data type: CatalogMetricsArtifact
        if not isinstance(v, CatalogMetricsArtifact):
            error_messages.append(f"Error! Input type `{type(v)}` is not `CatalogMetricsArtifact`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in CatalogArtifact with oneOf schemas: CatalogMetricsArtifact, CatalogModelArtifact. Details: " + ", ".join(error_messages))
        if match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in CatalogArtifact with oneOf schemas: CatalogMetricsArtifact, CatalogModelArtifact. Details: " + ", ".join(error_messages))
        return v

    @classmethod
    def from_dict(cls, obj: str | dict[str, Any]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # use oneOf discriminator to lookup the data type
        _data_type = json.loads(json_str).get("artifactType")
        if not _data_type:
            raise ValueError("Failed to lookup data type from the field `artifactType` in the input.")

        # check if data type is `CatalogMetricsArtifact`
        if _data_type == "metrics-artifact":
            instance.actual_instance = CatalogMetricsArtifact.from_json(json_str)
            return instance

        # check if data type is `CatalogModelArtifact`
        if _data_type == "model-artifact":
            instance.actual_instance = CatalogModelArtifact.from_json(json_str)
            return instance

        # deserialize data into CatalogModelArtifact
        try:
            instance.actual_instance = CatalogModelArtifact.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into CatalogMetricsArtifact
        try:
            instance.actual_instance = CatalogMetricsArtifact.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into CatalogArtifact with oneOf schemas: CatalogMetricsArtifact, CatalogModelArtifact. Details: " + ", ".join(error_messages))
        if match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into CatalogArtifact with oneOf schemas: CatalogMetricsArtifact, CatalogModelArtifact. Details: " + ", ".join(error_messages))
        return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        return json.dumps(self.actual_instance)

    def to_dict(self) -> dict[str, Any] | CatalogMetricsArtifact | CatalogModelArtifact | None:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        # primitive type
        return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


