
"""Catalog REST API

REST API for Kubeflow Hub Catalog

The version of the OpenAPI document: v1
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing_extensions import Self


class ToolCallingConfig(BaseModel):
    """Tool-calling arguments for this model.
    """ # noqa: E501
    tool_call_parser: StrictStr | None = Field(default=None, description="The tool-call parser identifier for the serving runtime.", alias="toolCallParser")
    chat_template: StrictStr | None = Field(default=None, description="Path to the chat template file packaged with the model.", alias="chatTemplate")
    enable_auto_tool_choice: StrictBool | None = Field(default=True, description="Whether to enable automatic tool choice in the serving runtime.", alias="enableAutoToolChoice")
    required_args: list[StrictStr] | None = Field(default=None, description='Additional CLI arguments required for tool calling beyond the parser, template, and auto-tool-choice flags. Each entry is a complete argument (e.g., "--config_format mistral").', alias="requiredArgs")
    __properties: ClassVar[list[str]] = ["toolCallParser", "chatTemplate", "enableAutoToolChoice", "requiredArgs"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self | None:
        """Create an instance of ToolCallingConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: dict[str, Any] | None) -> Self | None:
        """Create an instance of ToolCallingConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "toolCallParser": obj.get("toolCallParser"),
            "chatTemplate": obj.get("chatTemplate"),
            "enableAutoToolChoice": obj.get("enableAutoToolChoice") if obj.get("enableAutoToolChoice") is not None else True,
            "requiredArgs": obj.get("requiredArgs")
        })
        return _obj


