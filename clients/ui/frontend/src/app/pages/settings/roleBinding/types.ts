export enum RoleBindingPermissionsRBType {
  USER = 'User',
  GROUP = 'Group',
}

export enum RoleBindingPermissionsRoleType {
  EDIT = 'edit',
  ADMIN = 'admin',
  DEFAULT = 'default',
  CUSTOM = 'custom',
}
