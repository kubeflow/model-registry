---
description: Stop all model-registry dev environment processes (Frontend, BFF, Tilt) and optionally delete the Kind cluster
allowed-tools: Bash, Read, Write
argument-hint: [keep-cluster]
---

Read and follow the instructions in `.cursor/commands/kind-cluster-teardown.md`. That file is the single source of truth for this command. `$ARGUMENTS` are passed through as-is.
