---
description: Set up local Kind cluster for model-registry UI development with optional catalog and MinIO support
allowed-tools: Bash, Read, Write
argument-hint: [all|catalog|perf-data|minio|mock-k8s]
---

Read and follow the instructions in `.cursor/commands/kind-cluster-setup.md`. That file is the single source of truth for this command. `$ARGUMENTS` are passed through as-is.
