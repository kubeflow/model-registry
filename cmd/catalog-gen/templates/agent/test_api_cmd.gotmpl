Generate and run curl commands to test all API endpoints.

Read the skill file at .claude/skills/test-api.md for the complete list of curl commands.

Make sure the server is running first:
```
/run-local
```

Then use the curl commands from the skill file to test each endpoint.
