# Seed Test Data

This skill helps you populate sample data for testing the {{.CatalogName}} catalog.

## Entity Sample Data

Edit `manifests/kustomize/overlays/dev/sample-catalog.yaml`:

```yaml
{{.EntityNameLower}}s:
{{.EntitySample}}  - name: example-{{.EntityNameLower}}-2
    description: "Another example entity"
  - name: example-{{.EntityNameLower}}-3
    description: "Third example entity"
```
{{.ArtifactSamples}}
## Guidelines

1. **Entity Names**: Use lowercase with hyphens (e.g., `my-entity-name`)
2. **Artifact References**: The `{{.EntityNameLower}}Name` must match an entity name exactly
3. **Multiple Artifacts**: An entity can have multiple artifacts of the same type
4. **Required Fields**: Make sure all required properties are provided

## Verify Data Loading

After updating the files, start the server and test:

```bash
# Start server
go run ./cmd/{{.CatalogName}}.go --catalogs-path=manifests/kustomize/overlays/dev/sources.yaml

# List entities
curl -s "http://localhost:{{.Port}}/api/{{.CatalogName}}/v1alpha1/{{.EntityNameLower}}s" | jq

# Check specific entity
curl -s "http://localhost:{{.Port}}/api/{{.CatalogName}}/v1alpha1/{{.EntityNameLower}}s/example-{{.EntityNameLower}}-1" | jq
```
