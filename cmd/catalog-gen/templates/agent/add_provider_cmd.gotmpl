Add a new data provider to this catalog.

Usage: /add-provider <provider-type>

Example: /add-provider http
Example: /add-provider grpc
Example: /add-provider s3

Read the skill file at .claude/skills/add-provider.md and follow the instructions to:
1. Update catalog.yaml with the new provider
2. Create the provider implementation file
3. Register the provider in the main entry point

After making changes, verify with: go build ./...
