# Skills & Commands Reference

> **Auto-generated by catalog-gen** - Regenerate with `catalog-gen generate`

This document lists all available slash commands and skills for the {{.CatalogName}} catalog.

## Quick Reference

| Command | Description | Skill File |
|---------|-------------|------------|
{{.ArtifactCommands}}| `/fix-build` | Diagnose and fix compilation errors | - |
| `/add-property` | Add a new property to {{.EntityName}} | `add-property.md` |
| `/add-artifact` | Add a new artifact type | `add-artifact.md` |
| `/add-provider` | Add a new data provider | `add-provider.md` |
| `/regenerate` | Regenerate code and fix issues | - |
| `/run-local` | Start server locally | - |
| `/test-api` | Generate curl commands to test API | `test-api.md` |
| `/seed-data` | Populate sample test data | `seed-data.md` |

## Development Workflow

### Adding Features

1. **Add a property**: `/add-property <name> <type>`
2. **Add an artifact**: `/add-artifact <name>`
3. **Add a provider**: `/add-provider <type>`

### After Making Changes

1. **Regenerate**: `/regenerate`
2. **Fix errors**: `/fix-build`
3. **Test locally**: `/run-local`
4. **Test API**: `/test-api`

## Testing Workflow

1. **Seed test data**: `/seed-data`
2. **Start server**: `/run-local`
3. **Test endpoints**: `/test-api`

## Common Scenarios

### "I added a property to catalog.yaml"

```
/regenerate
```

### "The build is failing"

```
/fix-build
```

### "I want to test the API"

```
/seed-data
/run-local
/test-api
```
