Add a new artifact type to this catalog.

Usage: /add-artifact <artifact-name>

Example: /add-artifact Version
Example: /add-artifact License
Example: /add-artifact Deployment

Read the skill file at .claude/skills/add-artifact.md and follow all the steps to:

1. Update catalog.yaml with the new artifact definition
2. Run `catalog-gen generate` to generate artifact model/repository/loader
3. Run `make gen/openapi-server` to regenerate OpenAPI handlers
4. Update cmd/<catalog>.go to add artifact repository and change registry type
5. Update yaml_provider.go to use catalog.Artifact type and add parsing
6. Update api_<entity>_service_impl.go with endpoint and converter
7. Add sample data to artifacts.yaml

After making all changes, verify with: `go build ./...`
