Diagnose and fix compilation errors.

Usage: /fix-build

This will:
1. Run `go build ./...` and capture errors
2. Analyze error messages
3. Suggest or apply fixes

Run `go build ./...` to see current errors, then analyze and fix them.
