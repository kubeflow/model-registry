# Catalog sources configuration
# Each source defines where to fetch catalog data from.
catalogs:
  - id: sample
    name: Sample Source
    type: yaml
    enabled: true
    properties:
      yamlCatalogPath: /catalog/sample-catalog.yaml
